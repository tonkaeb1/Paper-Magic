# Paper Magic prototype

Open `index.html` in a browser to preview the site.

Navigation order:
Home → Training → Projects → About

This prototype uses plain HTML5 + CSS only and has no build process, database, plugins, or paid dependencies.
It is structured to work as a static GitHub Pages site.

Before publication:
- Replace placeholder training videos with @ProjectPaperMagic YouTube embeds.
- Add Cairstin's portrait and team acknowledgments.
- Replace the sample project with real project content.
- Review all wording with Cairstin.
- Add translations only after the English structure is stable.
